import React from 'react';
import { StyleSheet, View } from 'react-native';

const TextPostRowContainer = () => {
	return <View></View>;
};

const styles = StyleSheet.create({});

export default TextPostRowContainer;
