import React from 'react';
import { StyleSheet, View } from 'react-native';

const ArticlesRowContainer = () => {
	return <View></View>;
};

const styles = StyleSheet.create({});

export default ArticlesRowContainer;
